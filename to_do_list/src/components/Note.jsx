import React from "react";
import DeleteIcon from "@mui/icons-material/Delete";

const Note = (props) => {
  return (
    <div className="note">
      Note.
      <DeleteIcon />
    </div>
  );
};

export default Note;
