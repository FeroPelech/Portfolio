import React from "react";

const CharacterCard = () => {
  return (
    <div className="card">
      <img src={character.img} alt={character.name} />
    </div>
  );
};

export default CharacterCard;
